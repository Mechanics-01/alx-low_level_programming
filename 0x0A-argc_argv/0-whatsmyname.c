#include "main.h"

/**
 * main - prints program name, after which
 * it prints new line
 * @argc: argument number
 * @argv: argument vector
 * Return: integer
 */

int main(int argc  __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
