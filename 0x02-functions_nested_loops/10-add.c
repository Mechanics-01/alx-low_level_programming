#include "main.h"
/**
 * add - add twos integers and return the result
 * @a: first number argument
 * @b: second number argument
 * Return: results of the addtion of the two integers
 */

int add(int a, int b)
{
	int result = a + b;

	return (result);
}
